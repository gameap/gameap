/api/nodes:
  get:
    tags:
      - Nodes
    summary: List nodes
    description: |
      Returns a flat array of nodes with abbreviated fields (Admin only).
      The handler does not implement pagination; daemon credentials and
      script overrides are intentionally omitted from list items.
      Use `GET /api/nodes/{id}` for the full Node payload.
    operationId: listNodes
    responses:
      '200':
        description: List of nodes
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: '../schemas/NodeListItem.yaml'
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'
      '403':
        description: Forbidden
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'

/api/dedicated_servers:
  get:
    tags:
      - Nodes
    summary: List dedicated servers (alias)
    description: Alias for `GET /api/nodes` (Admin only). Same handler, no pagination.
    operationId: listDedicatedServers
    responses:
      '200':
        description: List of nodes
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: '../schemas/NodeListItem.yaml'
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'
      '403':
        description: Forbidden
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'

/api/nodes/summary:
  get:
    tags:
      - Nodes
    summary: Get nodes summary
    description: Returns summary statistics for nodes (Admin only)
    operationId: getNodesSummary
    responses:
      '200':
        description: Nodes summary
        content:
          application/json:
            schema:
              $ref: '../schemas/responses/NodesSummaryResponse.yaml'
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'
      '403':
        description: Forbidden
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'

/api/dedicated_servers/summary:
  get:
    tags:
      - Nodes
    summary: Get dedicated servers summary (alias)
    description: Alias for /api/nodes/summary (Admin only)
    operationId: getDedicatedServersSummary
    responses:
      '200':
        description: Nodes summary
        content:
          application/json:
            schema:
              $ref: '../schemas/responses/NodesSummaryResponse.yaml'
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'
      '403':
        description: Forbidden
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'

/api/nodes/setup:
  get:
    tags:
      - Nodes
    summary: Get node setup data
    description: |
      Returns setup data for adding a new node (Admin only).
      When gRPC is enabled, returns enrollment commands and connect URL.
      When gRPC is disabled, only the legacy `link` + `token` fields are
      meaningful.
    operationId: getNodesSetup
    responses:
      '200':
        description: Setup data
        content:
          application/json:
            schema:
              $ref: '../schemas/responses/NodeSetupResponse.yaml'
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'
      '403':
        description: Forbidden
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'

/api/dedicated_servers/setup:
  get:
    tags:
      - Nodes
    summary: Get node setup data (alias)
    description: Alias for `GET /api/nodes/setup` (Admin only). Same handler.
    operationId: getDedicatedServersSetup
    responses:
      '200':
        description: Setup data
        content:
          application/json:
            schema:
              $ref: '../schemas/responses/NodeSetupResponse.yaml'
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'
      '403':
        description: Forbidden
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'

/api/nodes/certificates.zip:
  get:
    tags:
      - Nodes
    summary: Download certificates
    description: Downloads client certificates as ZIP (Admin only)
    operationId: downloadNodesCertificates
    responses:
      '200':
        description: Certificates ZIP file
        content:
          application/zip:
            schema:
              type: string
              format: binary
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'
      '403':
        description: Forbidden
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'

/api/dedicated_servers/certificates.zip:
  get:
    tags:
      - Nodes
    summary: Download certificates (alias)
    description: Alias for /api/nodes/certificates.zip (Admin only)
    operationId: downloadDedicatedServersCertificates
    responses:
      '200':
        description: Certificates ZIP file
        content:
          application/zip:
            schema:
              type: string
              format: binary
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'
      '403':
        description: Forbidden
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'

/api/nodes/{id}:
  get:
    tags:
      - Nodes
    summary: Get node
    description: Returns a single node by ID (Admin only)
    operationId: getNode
    parameters:
      - $ref: '../parameters/PathNodeId.yaml'
    responses:
      '200':
        description: Node details
        content:
          application/json:
            schema:
              $ref: '../schemas/Node.yaml'
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'
      '403':
        description: Forbidden
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'
      '404':
        description: Node not found
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'

  put:
    tags:
      - Nodes
    summary: Update node
    description: Updates a node (Admin only)
    operationId: updateNode
    parameters:
      - $ref: '../parameters/PathNodeId.yaml'
      - $ref: '../parameters/HeaderIdempotencyKey.yaml'
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '../schemas/requests/UpdateNodeRequest.yaml'
    responses:
      '200':
        description: Node updated
        content:
          application/json:
            schema:
              $ref: '../schemas/Node.yaml'
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'
      '403':
        description: Forbidden
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'
      '404':
        description: Node not found
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'
      '422':
        description: Validation error
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'

  delete:
    tags:
      - Nodes
    summary: Delete node
    description: Deletes a node (Admin only)
    operationId: deleteNode
    parameters:
      - $ref: '../parameters/PathNodeId.yaml'
      - $ref: '../parameters/HeaderIdempotencyKey.yaml'
    responses:
      '204':
        description: Node deleted (no content)
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'
      '403':
        description: Forbidden
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'
      '404':
        description: Node not found
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'

/api/dedicated_servers/{id}:
  get:
    tags:
      - Nodes
    summary: Get dedicated server (alias)
    description: Alias for /api/nodes/{id} (Admin only)
    operationId: getDedicatedServer
    parameters:
      - $ref: '../parameters/PathNodeId.yaml'
    responses:
      '200':
        description: Node details
        content:
          application/json:
            schema:
              $ref: '../schemas/Node.yaml'
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'
      '403':
        description: Forbidden
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'
      '404':
        description: Not found
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'

  put:
    tags:
      - Nodes
    summary: Update dedicated server (alias)
    description: Alias for PUT /api/nodes/{id} (Admin only)
    operationId: updateDedicatedServer
    parameters:
      - $ref: '../parameters/PathNodeId.yaml'
      - $ref: '../parameters/HeaderIdempotencyKey.yaml'
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '../schemas/requests/UpdateNodeRequest.yaml'
    responses:
      '200':
        description: Node updated
        content:
          application/json:
            schema:
              $ref: '../schemas/Node.yaml'
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'
      '403':
        description: Forbidden
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'

  delete:
    tags:
      - Nodes
    summary: Delete dedicated server (alias)
    description: Alias for `DELETE /api/nodes/{id}` (Admin only)
    operationId: deleteDedicatedServer
    parameters:
      - $ref: '../parameters/PathNodeId.yaml'
      - $ref: '../parameters/HeaderIdempotencyKey.yaml'
    responses:
      '204':
        description: Node deleted (no content)
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'

/api/nodes/{node}/busy_ports:
  get:
    tags:
      - Nodes
    summary: Get busy ports
    description: |
      Returns ports in use on a node, grouped by IP (Admin only).
      The keys are IP strings; the values are arrays of integer ports.
    operationId: getNodeBusyPorts
    parameters:
      - name: node
        in: path
        required: true
        description: Node ID
        schema:
          type: integer
    responses:
      '200':
        description: Busy ports keyed by IP
        content:
          application/json:
            schema:
              type: object
              description: Map of IP address → list of ports in use on that IP
              additionalProperties:
                type: array
                items:
                  type: integer
              example:
                "0.0.0.0": [27015, 27016]
                "192.168.1.10": [25565]
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'
      '403':
        description: Forbidden
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'

/api/dedicated_servers/{node}/busy_ports:
  get:
    tags:
      - Nodes
    summary: Get busy ports (alias)
    description: Alias for `GET /api/nodes/{node}/busy_ports` (Admin only)
    operationId: getDedicatedServerBusyPorts
    parameters:
      - name: node
        in: path
        required: true
        description: Node ID
        schema:
          type: integer
    responses:
      '200':
        description: Busy ports keyed by IP
        content:
          application/json:
            schema:
              type: object
              additionalProperties:
                type: array
                items:
                  type: integer
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'

/api/nodes/{node}/ip_list:
  get:
    tags:
      - Nodes
    summary: Get IP list
    description: Returns IP addresses of a node (Admin only)
    operationId: getNodeIPList
    parameters:
      - name: node
        in: path
        required: true
        description: Node ID
        schema:
          type: integer
    responses:
      '200':
        description: IP addresses
        content:
          application/json:
            schema:
              type: array
              items:
                type: string
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'
      '403':
        description: Forbidden
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'

/api/dedicated_servers/{node}/ip_list:
  get:
    tags:
      - Nodes
    summary: Get IP list (alias)
    description: Alias for /api/nodes/{node}/ip_list (Admin only)
    operationId: getDedicatedServerIPList
    parameters:
      - name: node
        in: path
        required: true
        description: Node ID
        schema:
          type: integer
    responses:
      '200':
        description: IP addresses
        content:
          application/json:
            schema:
              type: array
              items:
                type: string
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'

/api/nodes/{id}/daemon:
  get:
    tags:
      - Nodes
    summary: Get daemon status
    description: Returns daemon status for a node (Admin only)
    operationId: getNodeDaemonStatus
    parameters:
      - $ref: '../parameters/PathNodeId.yaml'
    responses:
      '200':
        description: Daemon status
        content:
          application/json:
            schema:
              $ref: '../schemas/responses/NodeDaemonStatusResponse.yaml'
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'
      '403':
        description: Forbidden
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'

/api/dedicated_servers/{id}/daemon:
  get:
    tags:
      - Nodes
    summary: Get daemon status (alias)
    description: Alias for /api/nodes/{id}/daemon (Admin only)
    operationId: getDedicatedServerDaemonStatus
    parameters:
      - $ref: '../parameters/PathNodeId.yaml'
    responses:
      '200':
        description: Daemon status
        content:
          application/json:
            schema:
              $ref: '../schemas/responses/NodeDaemonStatusResponse.yaml'
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'

/api/nodes/{id}/logs.zip:
  get:
    tags:
      - Nodes
    summary: Download daemon logs
    description: Downloads daemon logs as ZIP (Admin only)
    operationId: downloadNodeLogs
    parameters:
      - $ref: '../parameters/PathNodeId.yaml'
    responses:
      '200':
        description: Logs ZIP file
        content:
          application/zip:
            schema:
              type: string
              format: binary
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'
      '403':
        description: Forbidden
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'

/api/dedicated_servers/{id}/logs.zip:
  get:
    tags:
      - Nodes
    summary: Download daemon logs (alias)
    description: Alias for /api/nodes/{id}/logs.zip (Admin only)
    operationId: downloadDedicatedServerLogs
    parameters:
      - $ref: '../parameters/PathNodeId.yaml'
    responses:
      '200':
        description: Logs ZIP file
        content:
          application/zip:
            schema:
              type: string
              format: binary
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../schemas/ErrorResponse.yaml'
