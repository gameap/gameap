package base

const (
	AutoSetupTokenCacheKey  = "node-auto-setup-token" //nolint:gosec // This is cache key, not a secret
	AutoCreateTokenCacheKey = "node-create-token"     //nolint:gosec // This is cache key, not a secret
)
